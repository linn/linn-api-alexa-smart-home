bin/
lib/
obj/
node_modules/
*.[Rr]e[Ss]harper*
*[Nn]crunch*
/MyLinn.sln.DotSettings.user
*.csproj.user
log.txt
*/logs/*
packages/*
.vs
.vscode
*.env
TestResult.xml
testrunner
nuget.exe
coverageConfig.json
coverageKnowns
TestResults/
App.config
config.json
tools/*.exe
*.md5sum
tools/Cake/*
tools/Addins/*
*.lock.json
*-lock.json
# ...but this repository's own lockfile MUST be committed. Without it nothing transitive was pinned,
# and @types/node drifted from 14 to 26 against a TypeScript that could not parse it - which is what
# killed the build. The rule above is inherited boilerplate; this exception is deliberate.
!package-lock.json
launchSettings.json
*.xproj.user
*.zip
*.download
!*/libds/*.zip
*.jsx
*.jsx.map
__pycache__
api/index.html
,serverless

# Compiled output and the assembled deployment package.
dist/
package/

# Agent tool scratch — per-user, never committed (linn/linn-api-development#394)
.serena/
.chunkhound/
.chunkhound.json
.cache/
